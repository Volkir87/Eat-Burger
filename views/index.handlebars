<div>
    <img src={{imageSource}}>
</div>
<div class="row">
    <div class="col-6 p-5">
        <ul class="list-group">
            {{#each burgers}}
            <li class="list-group-item d-flex justify-content-between">{{this.burger_name}}<button class="btn-primary" id={{this.id}}>Devour</button></li>
            {{/each}}
        </ul>
    </div>
    <div class="col-6 p-5">
        Right column will go here
    </div>
</div>
<div>
    <form>
        <label>Burger name:</label>
        <input id="name_input">
        <button id="add_button">Add burger</button>
    </form>

</div>
